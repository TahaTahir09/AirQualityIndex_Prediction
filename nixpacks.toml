[phases.setup]
nixPkgs = [
    "python311",
    "python311Packages.pip",
    "gcc"
]

[phases.install]
cmds = [
    "python -m venv /opt/venv",
    ". /opt/venv/bin/activate",
    "pip install --no-cache-dir -r requirements_backend.txt"
]

[start]
cmd = ". /opt/venv/bin/activate && gunicorn app:app --bind 0.0.0.0:${PORT:-8000}"
